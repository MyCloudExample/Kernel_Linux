#Para usar el modulo seguir estos pasos_

#1- Se puede compilar el modulo usando
    make all
#1- Por otro lado si se queire compilar y cargar el modulo en un solo paso
    make load
#2- Una vez cargado el modulo se puede verificar si se cargo
    make status
#3- Verificado los paso 1 y 2 se podran ejecutar las pruebas
    make test #enciende el led 2 segundo y luego lo apaga
    make test-on #enciende el led
    make test-off #apaga el led
    make test-blink #Blinky del led durante unos segundos
#4- Mostrar los logs del kernel
    make logs
#5- Se remueve el modulo del kernel
    make unload