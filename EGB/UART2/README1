#Antes de usar en la RAsoberry Pi 3 se debe deshabiliatar el BLuethoot

# 1. Editar el config.txt para deshabilitar Bluetooth y habilitar UART
sudo nano /boot/config.txt

# 2. Añadir estas líneas al final del archivo:
enable_uart=1
dtoverlay=disable-bt

# 3. Guardar y reiniciar
sudo reboot

#===========================================================================
#1 Compilacion del proyecto
    make all
#2 Construccion del devtree (solo una vez)
    make build-devtree 
#3 Carga del dev-tree (solo una vez)
    make load-devtree
#4 Carga del modulo
    make insmod
#5 Para remover el modulo hay que mataar procesos
    sudo pkill -9 -u nahuel
    #Reincia el SSH y luego se usa 
    make rmmod
    #Limpiar el buffer del kernel
    sudo dmesg -C